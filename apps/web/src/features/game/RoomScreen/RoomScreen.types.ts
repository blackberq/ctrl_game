// RoomScreen reads everything from the store; it takes no props.
export interface RoomScreenProps {
  _?: never;
}

export interface ModeOption {
  value: import("@ctrl-game/shared").GameMode;
  label: string;
}
